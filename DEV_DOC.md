# 随记 WhereNote 落地官网复刻开发文档

## 1. 需求分析与还原目标
高保真 1:1 像素级复刻 UI 设计稿，采用清新的浅绿/翡翠绿与薄荷绿配色方案（Nature Fresh Green），支持响应式适配与交互微动效。

### 1.1 页面模块拆解
1. **导航栏（Header / Navbar）**：
   - 品牌 Logo 与中英文双标题（“随记 WhereNote / 简单实用的收纳备忘”）
   - 导航链接（首页、功能、下载、关于）
   - 移动端汉堡菜单自适应

2. **Hero 首屏视觉区**：
   - 主副文案排版（“随手记一笔 东西放在哪里,一目了然”）
   - 3 大下载 / 体验入口按钮（App Store 下载、Android 下载、网页版 立即使用）
   - 右侧 3D 质感收纳箱与文具/定位几何立体插画视觉构图

3. **核心功能卡片区（Features Overview）**：
   - 区域副标题与主标题
   - 3 个轻质感立体悬浮卡片：
     - 记录物品位置（精确到家中任意位置）
     - 拍照存档（实物照片 + 备注）
     - 备注说明（自定义备注，细节补充）

4. **产品演示与核心卖点区（Interactive Showcase & Phone Mockup）**：
   - 左侧卖点列表与特色勾选项（随手一记、秒速查找、本地优先、清爽无广告）与“立即下载随记”CTA
   - 中间高精度 iPhone 真机模型（包含状态栏、记一笔表单、物品名称、存放位置、备注、实物照片插槽与交互按钮）
   - 右侧 3 个悬浮胶囊卡片（秒级模糊搜索、拍照归档、本地优先存储）

5. **底部页脚（Footer）**：
   - 浅绿圆角底色卡片，包含品牌摘要、快速导航、关注我们社交图标及 ICP 备案版权声明

## 2. 技术栈选型
- **核心框架**：React 18 + TypeScript + Vite
- **样式方案**：Tailwind CSS + CSS 自定义渐变与阴影体系
- **图标库**：Lucide React + 自定义精美 SVG 图标
- **代码规范**：严格类型检查、模块化组件拆分

## 3. 开发步骤与进度规划
- [x] 第一步：创建开发文档与项目基础规范
- [ ] 第二步：配置 `pnpm-workspace.yaml` 并初始化 `wotty/wherenote` 项目目录
- [ ] 第三步：搭建 Vite + React + TS + Tailwind CSS 基础架构与主题配色
- [ ] 第四步：构建精美立体 SVG 插画与拟物质感图标组件（3D 收纳箱、Pin、相机、记事本）
- [ ] 第五步：实现 Header、Hero 区域、核心功能卡片区
- [ ] 第六步：实现精细化手机表单 Mockup 与两侧卖点列表 / 胶囊卡片
- [ ] 第七步：实现 Footer 页脚与交互动效、响应式适配
- [x] 第八步：执行构建与代码质量校验
- [x] 第九步：执行本地 git 提交并记录版本

## 4. v0.0.2 Three.js 动态波浪背景

### 4.1 实现结构
- 使用 Three.js 正交相机、全屏平面与单通道片元着色器替换顶部静态 SVG。
- 着色器严格使用两条轮廓曲线生成薄荷绿后景与近白前景波浪，匹配设计稿的两层结构。
- 波浪边缘只保留约 1–2 个物理像素的抗锯齿过渡；柔和感来自层内纵向渐变，不使用大范围模糊软边。
- 前景波浪使用介于白底与后景之间的浅薄荷色阶，确保两层均可辨识而不影响内容对比度。
- 背景 Canvas 独立位于内容层之后，不接收指针事件，不改变现有页面布局与交互。

### 4.2 性能与可访问性
- 限制设备像素比，使用低功耗 WebGL 渲染器，并在页面不可见时暂停动画。
- 响应系统“减少动态效果”设置，启用后只渲染静态帧。
- 使用 `ResizeObserver` 适配桌面和移动端尺寸；组件卸载时释放动画、事件监听和 GPU 资源。

### 4.3 降级方案
- WebGL 初始化失败或上下文丢失时自动显示 CSS 浅绿渐变背景。
- React StrictMode 下每次 effect 都完整创建和销毁实例，避免重复 Canvas 与动画循环。
